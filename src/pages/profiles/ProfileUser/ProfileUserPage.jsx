import React from 'react';
import UserProfile from '../../../components/UserProfile/UserProfile'; // Импортируем компонент UserProfile

const ProfileUserPage = () => {
    return (
        <div>
            <UserProfile /> {/* Встраиваем компонент UserProfile */}
        </div>
    );
};

export default ProfileUserPage;
